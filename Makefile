all: info

info:
	@ echo "derwilly - c-exmaples"
	@ echo "\nYou run \"make\" from the base directory, as you are used to."
	@ echo "But you should run \"make\" from the applications directory instead!"
	@ echo "Switch to the applications directory and choose one that you like."
